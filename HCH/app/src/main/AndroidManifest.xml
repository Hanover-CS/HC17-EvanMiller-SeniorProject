<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.enm.hch">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BrowseListActivity">
            android:label="@string/browse_list"
        </activity>
        <activity android:name=".AboutActivity">
            android:label="@string/about_name"
        </activity>
        <activity android:name=".RoamCampusActivity">
            android:label="@string/roam_campus"
        </activity>
        <activity android:name=".AlphabeticalListingActivity" />
        <activity android:name=".AlphabeticalItemActivity" />
        <activity android:name=".CategoryListingActivity" />
        <activity android:name=".CategoryItemActivity"></activity>
    </application>

</manifest>